export const GEMINI_MODEL_TEXT = 'gemini-2.5-flash';

// Fallback image for video placeholders if needed
export const PLACEHOLDER_IMG = 'https://picsum.photos/300/200';

// Canvas resolution for stitching (Keeping it 720p for performance in browser)
export const CANVAS_WIDTH = 1280;
export const CANVAS_HEIGHT = 720;
export const VIDEO_FRAME_RATE = 30;
